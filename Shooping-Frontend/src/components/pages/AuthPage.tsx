import type React from "react";
import TempAuth from "../templates/templateAuth";

const Authpage: React.FC = () => {
    return (
        <>
            <TempAuth />
        </>
    );
}

export default Authpage;